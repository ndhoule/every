{
  "name": "every",
  "repo": "ndhoule/every",
  "version": "1.0.0",
  "description": "Check if a `predicate` function returns true for all values in a `collection`.",
  "keywords": [
    "functional",
    "all",
    "every"
  ],
  "license": "MIT",
  "scripts": [
    "index.js"
  ],
  "dependencies": {
    "ndhoule/each": "^1.0.3"
  }
}
